import { MapviewDirective } from './mapview.directive';

describe('MapviewDirective', () => {
  it('should create an instance', () => {
    const directive = new MapviewDirective();
    expect(directive).toBeTruthy();
  });
});
